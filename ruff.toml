[lint]
select = ["E", "W", "F"]
ignore = ["E501"]  # line-too-long: not enforced

[lint.per-file-ignores]
"deploy/core/config_model.py" = ["E302"]  # missing blank lines between top-level definitions
